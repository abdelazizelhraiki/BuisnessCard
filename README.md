# Business Card Website

A simple static website to display a business card image.

## Setup Instructions

1. **Add your business card image:**
   - Place your business card image in the root folder
   - Rename it to `carte-visite.jpg` (or update the filename in `index.html`)
   - Supported formats: JPG, PNG, GIF, WebP

2. **Test locally:**
   - Open `index.html` in your web browser to preview the site

## Deploy to GitHub Pages

### Step 1: Create a GitHub Repository

1. Go to [GitHub](https://github.com) and sign in
2. Click the "+" icon in the top right corner
3. Select "New repository"
4. Name your repository (e.g., `business-card` or `carte-visite`)
5. Choose "Public" visibility
6. Do NOT initialize with README, .gitignore, or license
7. Click "Create repository"

### Step 2: Push Your Code to GitHub

Open a terminal in this folder and run these commands:

```bash
# Initialize git repository
git init

# Add all files
git add .

# Commit files
git commit -m "Initial commit: Business card website"

# Add your GitHub repository as remote (replace with your username and repo name)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Push to GitHub
git branch -M main
git push -u origin main
```

### Step 3: Enable GitHub Pages

1. Go to your repository on GitHub
2. Click on "Settings" tab
3. Scroll down to "Pages" in the left sidebar
4. Under "Source", select "main" branch
5. Click "Save"
6. Wait a few minutes for deployment
7. Your site will be available at: `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/`

## Customization

- **Change colors:** Edit the gradient colors in `style.css` (line 8)
- **Change title:** Edit the `<h1>` text in `index.html`
- **Change image name:** Update the `src` attribute in `index.html` (line 13)

## Technologies Used

- HTML5
- CSS3
- GitHub Pages for hosting

## License

Feel free to use and modify this template for your own purposes.
# BuisnessCard
# BuisnessCard
"# BuisnessCard" 
# BuisnessCard
